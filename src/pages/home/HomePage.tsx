import { PageStub } from '@/shared/ui/PageStub';

// Mobile ref: HomeScreen.tsx
// Shows: avatar + greeting, hero progress card (X/N blocks), quick access (Results, Roadmap),
// roadmap block list with completed / current / locked states
export default function HomePage() {
  return (
    <PageStub
      title="Главная"
      description="Прогресс диагностики, дорожная карта блоков, быстрый доступ к результатам и роадмапу"
    />
  );
}
