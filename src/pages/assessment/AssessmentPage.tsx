import { PageStub } from '@/shared/ui/PageStub';

export default function AssessmentPage() {
  return (
    <PageStub
      title="Диагностика"
      description="Блоки вопросов: interests → thinking → personality → motivation → academic → directions"
    />
  );
}
