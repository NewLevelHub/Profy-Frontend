import { PageStub } from '@/shared/ui/PageStub';

export default function ResultLoadingPage() {
  return (
    <PageStub
      title="Анализируем результаты..."
      description="POST /result/generate → redirect to /results"
    />
  );
}
